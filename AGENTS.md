# Release Source Review

- 上游版本更新前，仅静态检查当前固定 SHA 到候选正式版本 SHA 的完整差异，以及其中新增或变更的依赖。
- 只判定恶意代码或恶意依赖：凭据或数据外传、隐蔽远程执行、破坏性行为、后门或持久化、挖矿等未授权资源滥用、混淆后执行、隐藏下载，以及有证据支持的依赖混淆、拼写仿冒或恶意安装脚本。
- 不检查或报告漏洞、Bug、正确性、代码质量、性能和可维护性；不进行漏洞扫描，也不运行测试。
- 只使用官方 release、tag/ref、compare、raw source 和依赖注册表元数据；必须将 tag 解析并固定到唯一完整提交 SHA，禁止以分支或未解析的 tag 对象作为审查目标。
- 不 clone、安装、构建或运行上游代码和发布产物。若完整 diff、二进制/LFS、子模块、依赖来源或安装脚本无法静态核验，结论为暂缓，不得更新 pin。
- 每个变更文件都必须检查；依赖只检查本次新增、升级或来源变更的条目。发现必须引用不可变 SHA、文件和具体代码证据，不得用项目声誉、CI 结果、release notes 或包名相似度代替证据。
- 审查结论仅为：通过、暂缓、无法核验或无新版。只有“通过”的组件可以更新工作流 pin 和对应文档。
- `.omo/` 永不暂存或提交。每个提交必须且只能包含一次 `Co-authored-by: naiba/CloudCode <hi+cloudcode@nai.ba>` trailer。
